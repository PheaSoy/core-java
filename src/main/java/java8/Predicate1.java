package java8;

import org.apache.log4j.Logger;

import java.util.stream.Stream;

public class Predicate1 {
    static Logger logger = Logger.getLogger(Predicate1.class);

    static Stream myStream = Stream.of(1, 2, 3, 4, 5, 6, 7, 8, 9);

}
